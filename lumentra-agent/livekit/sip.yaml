# LiveKit SIP Server Configuration
# Bridges SIP calls from SignalWire to LiveKit rooms

log_level: info

# LiveKit server connection
# api_key/api_secret are injected at runtime via environment variables.
ws_url: ws://localhost:7880

# Redis (must match LiveKit server)
redis:
  address: localhost:6379

# SIP signaling port
sip_port: 5060

# RTP media port range
rtp_port: 10000-20000
